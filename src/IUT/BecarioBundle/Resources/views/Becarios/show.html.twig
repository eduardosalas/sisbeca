{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Información Becario</h1>
    <div class="row">
    <div class="table-responsive">
        <table class="table table-bordered">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ entity.id }}</td>
                </tr>
                <tr>
                    <th>Cedula</th>
                    <td>{{ entity.cedula }}</td>
                </tr>
                <tr>
                    <th>Username</th>
                    <td>{{ entity.username }}</td>
                </tr>
                <tr>
                    <th>Nombre1</th>
                    <td>{{ entity.nombre1 }}</td>
                </tr>
                <tr>
                    <th>Nombre2</th>
                    <td>{{ entity.nombre2 }}</td>
                </tr>
                <tr>
                    <th>Ape1</th>
                    <td>{{ entity.ape1 }}</td>
                </tr>
                <tr>
                    <th>Ape2</th>
                    <td>{{ entity.ape2 }}</td>
                </tr>
                <tr>
                    <th>Codcarrera</th>
                    <td>{{ entity.codCarrera }}</td>
                </tr>
            </tbody>
        </table>
    </div>
    </div>
    <div class="row">
            <a class="btn btn-info col-lg-2 col-lg-offset-3 col-md-2 col-md-offset-3 col-sm-2 col-sm-offset-3" role="button"  href="{{ path('becarios') }}">
                Volver a la lista
            </a>
        
            <a class="btn btn-info col-lg-2 col-lg-offset-3 col-md-2 col-md-offset-3 col-sm-2 col-sm-offset-3" role="button"  href="{{ path('becarios_edit', { 'id': entity.id }) }}">
                Editar
            </a>
        {{ form(delete_form) }}
    </div>
{% endblock %}
